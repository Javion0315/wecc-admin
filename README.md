## Quick start

Clone `slgesc` repo:

```bash
$ git clone https://github.com/Javion0315/slgesc.git
```

Install dependencies:

```bash
$ cd slgesc
$ yarn install
```

Run `serve`:

```bash
$ yarn run serve
```

## Deploy Build Dist
```bash
$ yarn run generate
```
